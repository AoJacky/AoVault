### 实测时序
现在实测到的时序：
	1. 先有start 信号
	2. 经过e3_sync_bit 模块后输出 s_start_sync 信号
	3. s_start_sync 下降沿后o_soen 置高
	4. 经过一个周期后，o_status 进入 idle 状态
	5. 经过两个周期后 o_tx_rdy 置高
	6. o_tx_rdy 置低时 o_sdout 开始传输 request
	7. 传输结束后，i_sdin 出现一个短暂的上升沿脉冲，o_status 变成 9
但是 response 始终没有任何信号